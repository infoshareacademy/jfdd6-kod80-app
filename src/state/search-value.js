//to co w counter.js

const initialState = {
  date: null,
  places: null,
  music: null,
  concert: null
}

const reducer = ( state = initialState, action = {}) => {
  switch (action.type) {
    default: return state
  }
}

export default reducer
